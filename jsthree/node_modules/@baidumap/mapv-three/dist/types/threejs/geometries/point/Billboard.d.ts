/**
 * 和Plane类似，但是是立着的平面
 */
export class InstancedBillboardBufferGeometry extends CustomInstancedBufferGeometry {
    setModelData: () => void;
}
import { CustomInstancedBufferGeometry } from '../../core/CustomInstancedBufferGeometry';
