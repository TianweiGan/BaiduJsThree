/**
 * 增强的GLTF加载器，支持Meshopt、DRACO和KTX2等扩展。
 *
 * 已实例化，可直接使用。
 *
 * @example
 * ```javascript
 * const loader = mapvthree.gltfLoader;
 * loader.load(url, (gltf) => {
 *     console.log(gltf);
 * });
 * ```
 */
export let gltfLoader: GLTFLoader;
export function createEnhancedLoader(): GLTFLoader;
import { GLTFLoader } from './GLTFLoader';
