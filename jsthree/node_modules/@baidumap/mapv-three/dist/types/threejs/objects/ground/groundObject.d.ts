/**
 * 贴地绘制
 * ```javascript
 * const groundObject = new GroundObject({
 *     type: 'Polygon',
 *     color: '#ff0000',
 *     opacity: 0.5,
 * }, engine);
 * const dataSource = await mapvthree.GeoJSONDataSource.fromObject('polygon.geojson');
 * groundObject.dataSource = dataSource;
 * // 销毁
 * groundObject.destroy();j
 * ```
 */
export class GroundObject {
    /**
     * @param {{color?: string, minHeight?: number, maxHeight?: number, nearScale?: number
     * type: string, opacity?: number,
     * }} parameter 参数对象
     * @param {string} parameter.type 类型, 可选值: Polygon, Line
     * @param {number} parameter.minHeight 最小高度, 默认 -1000，只贴在minHeight到maxHeight之间的对象
     * @param {number} parameter.maxHeight 最大高度, 默认 1000，只贴在minHeight到maxHeight之间的对象
     * @param {number} parameter.nearScale 近平面缩放, 默认 1
     * @param {string} parameter.color 颜色
     * @param {number} parameter.opacity 不透明度
     * @param {*} engine
     */
    constructor(parameter: {
        color?: string;
        minHeight?: number;
        maxHeight?: number;
        nearScale?: number;
        type: string;
        opacity?: number;
    }, engine: any);
    /** @private */ private _dataSource;
    engine: any;
    parameters: {
        color?: string;
        minHeight?: number;
        maxHeight?: number;
        nearScale?: number;
        type: string;
        opacity?: number;
    };
    _renderOrder: number;
    initPolygonObject(): void;
    frontObject: Polygon | undefined;
    backObject: Polygon | undefined;
    renderObject: any;
    initLineObject(): void;
    set dataSource(value: any);
    get dataSource(): any;
    set renderOrder(value: number);
    get renderOrder(): number;
    addEventListener(type: any, callback: any): void;
    removeEventListener(type: any, callback: any): void;
    destroy(): void;
}
import { Polygon } from '../../objects/polygon/Polygon';
