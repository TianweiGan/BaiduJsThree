/**
 * 瓦片信号灯，这是同一个方向上的几个灯态组合
 */
export class TileLampGroup extends TrafficGroup {
    constructor(count: any, id: any, options?: {});
    useTable: boolean;
    /** @private */
    private _id;
    /** @private */
    private _sizeAttention;
    /** @private */
    private _pixelHeight;
    isTileLampGroup: boolean;
    _initLight(count: any, options?: {}): void;
    onBeforeScenePrepareRender(engine: any, scene: any, camera: any, renderState: any): void;
    updateState(time: any): void;
    get lightId(): string;
    set timeTable(timeTable: any);
    get timeTable(): any;
    _timeTable: any;
    raycast(raycaster: any, intersects: any): void;
}
/**
 * 每一个灯是一个TileLamp的实例
 * 每个灯态都是竖向的3个图标展示：颜色、方向、倒计时
 *
 * timeTable: {
 *     startTime: 1724931141000,
 *     reuse: true,
 *     iconOffset: 1,
 *     table: [
 *         {
 *             stats: 1,
 *             period: 40,
 *             text: false,
 *         },
 *         {
 *             status: 2,
 *             period: 20,
 *             text: false,
 *         }
 *     ]
 * }
 */
export class TileLamp {
    constructor(width: any, height: any, options?: {});
    isMesh: boolean;
    isTileLamp: boolean;
    geometry: any;
    material: TileTrafficLightMaterial;
    updateState(time: any): void;
    set timeTable(timeTable: any);
    get timeTable(): any;
    _timeTable: any;
    raycast(raycaster: any, intersects: any): void;
}
import { TrafficGroup } from '../../../addons/traffic/objects/TrafficGroup';
import { TileTrafficLightMaterial } from '../../materials/element/TileTrafficLightMaterial';
