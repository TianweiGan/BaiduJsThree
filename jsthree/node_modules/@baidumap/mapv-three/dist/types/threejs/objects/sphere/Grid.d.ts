/**
 * ![](media://grid.png)
 * ```javascript
 * const grid = engine.add(new mapvthree.Grid({
 *  isMultiColor: true, // 是否多色渐变
 *  multiColor: ['#8dffd0', '#0095DA'], // 渐变柱颜色数组
 *  height: 250, // 菱柱高度
 *  radialSegments: 6, // 边数
 *  heightSegments: 1, // 沿圆柱高的分段切片
 *  radiusTop: 20, // 顶面半径
 *  radiusBottom: 20, // 底面半径
 *  openEnded: false, // 圆柱顶底是否封闭
 *  isLight: false, // 是否为光柱
 *  }));
 * ```
 */
export class Grid extends GeoInstancedPointMesh {
    /**
     * 构造函数
     * @param {{
     * color?: string, height?: number, radialSegments: number
     * radiusTop?: number, radiusBottom?: number, openEnded?: boolean, heightSegment?: number,
     * isMultiColor?: boolean, multiColor?: Array<string>, isLight?: boolean, lightPos?: number
     * }} parameters 传入参数对象
     * @param {string} parameters.color 颜色
     * @param {number} parameters.height 高度
     * @param {number} parameters.radialSegments 边数
     * @param {number} parameters.radiusTop 顶面半径
     * @param {number} parameters.radiusBottom 底面半径
     * @param {boolean} parameters.openEnded 顶面是否封闭
     * @param {number} parameters.heightSegments 沿圆柱高的分段切片
     * @param {boolean} parameters.isMultiColor 是否多色渐变
     * @param {Array<string>} parameters.multiColor 渐变颜色数组, heightSegments+1
     * @param {boolean} parameters.isLight 是否为光柱(底部渐变透明)
     * @param {number} parameters.lightPos 光柱渐变参数
     */
    constructor(parameters: {
        color?: string;
        height?: number;
        radialSegments: number;
        radiusTop?: number;
        radiusBottom?: number;
        openEnded?: boolean;
        heightSegment?: number;
        isMultiColor?: boolean;
        multiColor?: Array<string>;
        isLight?: boolean;
        lightPos?: number;
    });
    /** 颜色 */ color: any;
    /** 柱体高度 */ height: any;
    /** 柱体的边数 */ radialSegments: any;
    /** 顶面半径 */ radiusTop: any;
    /** 底面半径 */ radiusBottom: any;
    /** 圆柱顶底是否封闭 */ openEnded: any;
    /** 沿圆柱高的分段切片 */ heightSegments: any;
    /** 是否多色渐变 */ isMultiColor: any;
    /** 渐变柱颜色数组, heightSegments+1 */ multiColor: any;
    /** 是否为光柱(底部渐变透明) */ isLight: any;
    /** 光柱渐变参数 */ lightPos: any;
    parameters: {
        color?: string;
        height?: number;
        radialSegments: number;
        radiusTop?: number;
        radiusBottom?: number;
        openEnded?: boolean;
        heightSegment?: number;
        isMultiColor?: boolean;
        multiColor?: Array<string>;
        isLight?: boolean;
        lightPos?: number;
    };
    /**
     * @internal
     * 计算高度拉升模型矩阵
     */
    getInstanceLocalMatrix: (coordinates: any, dataItem: any, index: any) => any;
}
import { GeoInstancedPointMesh } from '../point/GeoInstancedPointMesh';
