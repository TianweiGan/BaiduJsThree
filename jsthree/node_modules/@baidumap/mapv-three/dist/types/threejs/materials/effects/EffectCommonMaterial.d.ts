export class EffectCommonMaterial extends CommonShaderMaterial {
    type: string;
    isEffectMaterial: boolean;
    lights: boolean;
    fog: boolean;
    defines: {
        USE_UV: boolean;
    };
    emissiveEnabled: boolean;
    emissive: number[];
    /**
     * @internal
     * 在 rendering.handleMaterialBeforeCompile 中会将 uniforms 中的属性添加到 material 中
     */
    handleMaterialBeforeCompile(engine: any, rendering: any): void;
}
import { CommonShaderMaterial } from '../CommonShaderMaterial';
