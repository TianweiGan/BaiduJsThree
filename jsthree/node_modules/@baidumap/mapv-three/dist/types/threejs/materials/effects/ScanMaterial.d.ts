/**
 * 扫描特效动画材质，由一个扫描动画和一个纹理动画组成，可通过配置动画的参数和纹理动画的参数来实现不同的效果
 * @example 普通扫描特效动画材质
 * ```
 * var material = new mapvthree.ScanMaterial({
 *      color: new THREE.Color(0x66aadd),
 *      size: [100, 40],
 *      speed: 500,
 *      direction: mapvthree.ScanMaterial.DIRECTION_VERTICAL,
 *      emissive: new THREE.Color(0x001144),
 * });
 * ```
 * @example 带纹理打底的扫描特效动画材质
 * ```
 * var material = new mapvthree.ScanMaterial({
 *      mapSrc: 'website_assets/floor/radar.png',
 *      mapSize: [5, 10],
 *      mapSpeed: 40,
 *      color: new THREE.Color('#fff'),
 *      size: [36, 36],
 *      sizeRatio: 0.7,
 *      speed: 80,
 *      direction: mapvthree.ScanMaterial.DIRECTION_VERTICAL,
 *      emissive: new THREE.Color(0x002288),
 * });
 * ```
 * @example 带纹理遮罩的扫描特效动画材质
 * ```
 * var material = new mapvthree.ScanMaterial({
 *      maskMap: new THREE.TextureLoader().load('website_assets/floor/mask.png'),
 *      color: new THREE.Color('#fff'),
 *      size: [1, 0.05],
 *      speed: 0.1,
 *      emissive: new THREE.Color(0x001155),
 * });
 * ```
 */
export class ScanMaterial extends EffectCommonMaterial {
    static DIRECTION_VERTICAL: number;
    static DIRECTION_HORIZONTAL: number;
    /**
     * 构造函数
     * @param {{
    * direction?: number, speed?: number, size?: Array<number>, sizeRatio?: number,
    * color?: string | number | THREE.Color, opacity?: number, emissive?: string | number | THREE.Color,
    * mapSrc?: string, mapColor?: Array<number>, mapSpeed?: number, mapSize?: Array<number>,
    * maskMap?: THREE.Texture
    * }} parameters 材质参数，所有被Material类及其子类接受的参数。
    * @param {number} parameters.direction 扫描动画方向，可选值有`ScanMaterial.DIRECTION_VERTICAL`和`ScanMaterial.DIRECTION_HORIZONTAL`
    * @param {number} parameters.speed 扫描动画速度，默认值为1
    * @param {Array<number>} parameters.size 整体效果尺寸大小，默认值为[1, 1]，单位为米
    * @param {number} parameters.sizeRatio 扫描动画部分占整体的比例值，默认1填满，比如0.5表示特效占整体的1/2
    * @param {string | number | THREE.Color} parameters.color 动画效果颜色，默认值为0xffffff
    * @param {number} parameters.opacity 整体效果透明度，默认值为1
    * @param {string | number | THREE.Color} parameters.emissive 自发光颜色，默认值为0x000000，即不发光
    * @param {string} parameters.mapSrc 纹理动画的纹理图片地址，如果不设置则没有纹理动画
    * @param {Array<number>} parameters.mapColor 纹理动画的颜色，默认值为[1, 1, 1]
    * @param {number} parameters.mapSpeed 纹理动画的速度，默认值为1
    * @param {Array<number>} parameters.mapSize 纹理动画的尺寸，默认值为[1, 1]，单位为米
    * @param {THREE.Texture} parameters.maskMap 动画的遮罩图层，预期传入一张透明遮罩纹理，透明部分为不显示的部分，不透明部分为显示的部分，不设置则没有遮罩效果
    */
    constructor(parameters: {
        direction?: number;
        speed?: number;
        size?: Array<number>;
        sizeRatio?: number;
        color?: string | number | THREE.Color;
        opacity?: number;
        emissive?: string | number | THREE.Color;
        mapSrc?: string;
        mapColor?: Array<number>;
        mapSpeed?: number;
        mapSize?: Array<number>;
        maskMap?: THREE.Texture;
    });
    name: string;
    transparent: boolean;
    fragmentShader: any;
    vertexShader: any;
}
import { EffectCommonMaterial } from './EffectCommonMaterial';
