/**
 * 设备加载方式，需用用它的子类执行（ES6没有抽象类）
 */
export class AbstractLoadPolicy {
    constructor(options?: {});
    _policyOptions: {};
    /**
     * 获取设备列表
     * @param config
     * @returns {Promise<*[]>}
     */
    getDeviceList(config: any): Promise<any[]>;
}
