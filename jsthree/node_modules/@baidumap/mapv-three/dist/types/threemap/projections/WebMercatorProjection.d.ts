/**
 * Web墨卡托投影(EPSG:3857)，常用于网络地图服务
 *
 * Web墨卡托投影是一种常用的地图投影，在大多数网络地图服务中使用，如Google Maps、OpenStreetMap等。
 * 它将地球投影到正方形平面上，具有以下特点：
 * - 等角性：保持局部形状
 * - 不保持面积比例，高纬度地区面积被放大
 * - 经线和纬线都是直线
 * - 坐标单位为米
 *
 * 投影有效范围为经度±180度，纬度约±85.05度。
 *
 * @category Internal
 */
export class WebMercatorProjection extends Projection {
    /**
     * 投影名称
     * @type {string}
     * @readonly
     */
    readonly name: string;
}
import { Projection } from './Projection';
