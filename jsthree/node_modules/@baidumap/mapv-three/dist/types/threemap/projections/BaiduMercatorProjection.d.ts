/**
 * 百度墨卡托投影，用于百度地图的坐标系统
 *
 * 百度墨卡托投影是基于Web墨卡托投影的变体，专用于百度地图服务。
 * 它对标准墨卡托投影进行了偏移和变换，使其与百度地图系统匹配。
 * 特点：
 * - 坐标系原点位于中国地区
 * - 坐标单位为米
 * - 与WGS84坐标系存在非线性偏移
 *
 * 该投影主要用于：
 * - 与百度地图服务对接
 * - 处理百度地图提供的数据
 * - 开发基于百度地图的应用
 *
 * @category Internal
 */
export class BaiduMercatorProjection extends Projection {
    /**
     * 投影名称
     * @type {string}
     * @readonly
     */
    readonly name: string;
}
import { Projection } from './Projection';
