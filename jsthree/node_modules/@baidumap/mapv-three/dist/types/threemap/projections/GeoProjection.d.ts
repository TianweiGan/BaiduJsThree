/**
 * 地理经纬度投影，将经纬度坐标按比例缩放
 *
 * GeoProjection是最简单的投影方式，它仅将经纬度坐标按一定比例缩放，不改变坐标的拓扑关系。
 * 缩放系数为地球半径(6378137米)乘以π/180，将角度单位转换为近似的米单位。
 * 该投影主要用于：
 * - 简单的地理数据可视化
 * - 不要求高精度的场景
 * - 作为其他投影的基础
 *
 * @category Internal
 */
export class GeoProjection extends Projection {
    /**
     * 投影名称
     * @type {string}
     * @readonly
     */
    readonly name: string;
}
import { Projection } from './Projection';
