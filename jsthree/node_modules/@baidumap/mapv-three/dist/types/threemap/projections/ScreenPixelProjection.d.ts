/**
 * 屏幕像素投影，Y轴反的
 * @class ScreenPixelProjection
 * @extends {Projection}
 */
export class ScreenPixelProjection extends Projection {
    name: string;
    isAxisAligned: boolean;
    projectCoordinate(input: any, output: any): any;
    unprojectCoordinate(input: any, output: any): any;
}
import { Projection } from './Projection';
