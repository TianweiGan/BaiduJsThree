/**
 * 地球中心地固坐标系(ECEF)投影，用于3D地球表示
 *
 * ECEF坐标系是一种以地球中心为原点的笛卡尔坐标系：
 * - X轴通过赤道面和本初子午线(0°经线)的交点
 * - Z轴通过北极
 * - Y轴与X和Z轴垂直，形成右手坐标系
 *
 * 特点：
 * - 坐标单位为米
 * - 可以精确表示地球曲面
 * - 适用于全球尺度的3D可视化
 * - 随地球自转而旋转
 *
 * 该投影主要用于：
 * - 3D地球表示
 * - 卫星轨道计算
 * - 精确的全球定位
 *
 * @category Internal
 */
export class ECEFProjection extends Projection {
    /**
     * 投影名称
     * @type {string}
     * @readonly
     */
    readonly name: string;
    /**
     * 将地理边界框转换为投影坐标系边界框
     * @param {Box3} geoBox 地理坐标系中的边界框
     * @param {OrientedBoundingBox} [projectedBox] 可选的输出边界框，如不提供则创建新的OrientedBoundingBox
     * @returns {OrientedBoundingBox} 投影坐标系中的有向边界框
     */
    geoBoxToProjectedBox(geoBox: Box3, projectedBox?: OrientedBoundingBox): OrientedBoundingBox;
    /**
     * 获取地理边界框的LOD缩放因子
     * @param {Box3} geoBoundingBox 地理坐标系中的边界框
     * @returns {number} LOD缩放因子
     */
    getLODSacleOfGeoBoundingBox(geoBoundingBox: Box3): number;
    localFrameToFixedFrame(origin: any, fixedFrame: any): any;
}
import { Projection } from './Projection';
import { OrientedBoundingBox } from '../engine/map/earth/OrientedBoundingBox';
