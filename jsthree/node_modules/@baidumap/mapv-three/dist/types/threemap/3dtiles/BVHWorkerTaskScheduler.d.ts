export class BVHWorkerTaskScheduler extends WorkerTaskScheduler {
    constructor(size: any);
    /** @private */
    private _currentId;
    /** @private */
    private _geometryMap;
    generateAsync(geometry: any, options: any): Promise<any>;
    onMessageCompleted(data: any, message: any): any;
}
import { WorkerTaskScheduler } from '../../util/WorkerTaskScheduler';
