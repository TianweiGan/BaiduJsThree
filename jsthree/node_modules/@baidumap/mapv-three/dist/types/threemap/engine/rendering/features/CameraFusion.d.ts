export class FeatureCameraFusion extends Feature {
    /** @private */
    private _lastEnabled;
    /** @private */
    private _camera;
    /** @private */
    private _texture;
    /** @private */
    private _opacity;
    _init(): void;
    _renderPass: CameraFusionPass | null | undefined;
    afterRender(): void;
    dispose(): void;
    set camera(camera: null);
    get camera(): null;
    set texture(texture: null);
    get texture(): null;
    set opacity(opacity: number);
    get opacity(): number;
}
import { Feature } from './Feature.js';
import { CameraFusionPass } from '../postprocessing/CameraFusionPass';
