/**
 * geoLocate
 *
 * ```javascript
 * const geoLocate = new GeoLocate(contorlContainer, enable, engine);
 * ```
 */
export class GeoLocate extends BaseWidgets {
    /**
     * 创建子节点
     */
    createDom(): any;
    locate(): void;
}
import { BaseWidgets } from './BaseWidgets';
