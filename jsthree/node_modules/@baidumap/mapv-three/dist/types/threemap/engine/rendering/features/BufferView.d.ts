export class FeatureBufferView extends Feature {
    /** @private */
    private _lastEnabled;
    /** @private */
    private _offset;
    /** @private */
    private _currentFrameTextures;
    _init(): void;
    _renderPass: AddTexturePass | undefined;
    afterRender(): void;
    addCurrentFrameTexture(texture: any): void;
    dispose(): void;
    set offset(offset: number);
    get offset(): number;
}
import { Feature } from './Feature.js';
import { AddTexturePass } from '../postprocessing/AddTexturePass';
