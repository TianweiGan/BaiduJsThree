/**
 * scale
 *
 * ```javascript
 * const scale = new Scale(contorlContainer, enable, engine);
 * ```
 */
export class Scale extends BaseWidgets {
    /**
     * 创建子节点
     */
    createDom(): any;
    _text: HTMLDivElement | undefined;
    /** @private */
    private calculateScale;
}
import { BaseWidgets } from './BaseWidgets';
