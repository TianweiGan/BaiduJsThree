/**
 * logo
 *
 * ```javascript
 * const logo = new Logo(contorlContainer, enable, engine);
 * ```
 */
export class Logo extends BaseWidgets {
    /**
     * 设置logo的图片
     */
    set url(url: any);
    /**
     * logo的图片
     */
    get url(): any;
    /**
     * 创建子节点
     */
    createDom(): any;
    _url: any;
}
import { BaseWidgets } from './BaseWidgets';
