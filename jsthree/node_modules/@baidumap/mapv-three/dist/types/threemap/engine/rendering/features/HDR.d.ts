/**
 * HDR
 * 未实现，暂时占用配置项
 */
export class FeatureHDR extends Feature {
    /** @private */
    private _bufferType;
    /** @private */
    private _exposure;
    /** @private */
    private _lastEnabled;
    needsFloatRenderTarget: boolean;
    set exposure(value: number);
    get exposure(): number;
}
import { Feature } from './Feature';
