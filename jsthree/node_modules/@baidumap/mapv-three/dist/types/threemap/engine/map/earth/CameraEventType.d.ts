declare const _default: Readonly<{
    /**
     * A left mouse button press followed by moving the mouse and releasing the button.
     *
     * @type {number}
     * @constant
     */
    LEFT_DRAG: number;
    /**
     *  A right mouse button press followed by moving the mouse and releasing the button.
     *
     * @type {number}
     * @constant
     */
    RIGHT_DRAG: number;
    /**
     *  A middle mouse button press followed by moving the mouse and releasing the button.
     *
     * @type {number}
     * @constant
     */
    MIDDLE_DRAG: number;
    /**
     * Scrolling the middle mouse button.
     *
     * @type {number}
     * @constant
     */
    WHEEL: number;
    /**
     * A two-finger touch on a touch surface.
     *
     * @type {number}
     * @constant
     */
    PINCH: number;
}>;
export default _default;
/**
 * Enumerates the available input for interacting with the camera.
 */
export type CameraEventType = number;
export namespace CameraEventType {
    let LEFT_DRAG: number;
    let RIGHT_DRAG: number;
    let MIDDLE_DRAG: number;
    let WHEEL: number;
    let PINCH: number;
}
