/**
 * 点测量
 */
export class PointMeasure extends Measure {
    /**
     * @param { Object } options 绘制点参数
     * @param {string} options.pointColor 点颜色,默认为'rgb(200, 200, 50)'
     * @param {string} options.pointSize 点尺寸，默认为10
     * @param {number} options.fixedNum 保留几位小数
     * @param {boolean} options.showLabel 是否展示测量信息
     */
    constructor(engine: any, options?: {
        pointColor: string;
        pointSize: string;
        fixedNum: number;
        showLabel: boolean;
    });
    /** @priuvate */ _editor: null;
    /** @private */
    private _infoMarkerData;
    _engine: any;
    _options: {
        pointColor: string;
        pointSize: string;
        fixedNum: number;
        showLabel: boolean;
    };
    _showLabel: any;
    fixedNum: any;
    /** @private */
    private initEditor;
    /**
     * @private
     * 测量信息展示，默认DomPoint，可以自定义
     */
    private initInfoMarker;
    _infoMarker: DomPoints | undefined;
    _infoMarkerDataSource: JSONDataSource | undefined;
    createInfoMarkerDom(feature: any): HTMLDivElement;
    createInfoMarker: () => DomPoints;
    _node: any;
    clearBuffer(): void;
    /**
     * 清除绘制元素
     */
    clearAll(): void;
    /** @private */
    private dispose;
    set showLabel(value: any);
    get showLabel(): any;
}
import { Measure } from './Measure';
import { DomPoints } from '../../../threejs/objects/point/DomPoint';
import { JSONDataSource } from '../../dataSource/JSONDataSource';
