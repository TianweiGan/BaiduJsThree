/**
 * 样条编辑器
 * ```js
 * const tile = engine.add(new mapvthree.Default3DTiles({
 *   url: tilesetUrl,
 * }))
 * tile.addEventListener('click', () => {});
 * const lineEditor = engine.add(new mapvthree.SplineEditor(engine, {
 *   virtualLineParameter: {
 *      color: 'red',
 *      lineWidth: 2,
 *   },
 *   lineParameter: {
 *      color: 'green',
 *      lineWidth: 5,
 *   }
 * }));
 * // 贴地绘制线
 * const lineEditor = engine.add(new mapvthree.SplineEditor(engine, {
 *    clampToGround: true,
 * }))
 * ```
 * ![](media://lineEditor.png)
 */
export class SplineEditor extends Editor {
    /**
     * @param { Object } options 绘制参数
     * @param {string} options.lineColor 线颜色,默认为'rgb(200, 200, 50)'
     * @param {Object} options.virtualLineParameter 虚拟线参数，用来配置虚拟线的颜色、线宽等
     * @param {Object} options.lineParameter 线参数，用来配置线的颜色、线宽等
     * @param {boolean} options.clampToGround 是否贴地
     */
    constructor(engine: any, options?: {
        lineColor: string;
        virtualLineParameter: Object;
        lineParameter: Object;
        clampToGround: boolean;
    });
    /** @private */ private _virtualLineData;
    /** @private */ private _splineData;
    /** @private */ private _helpersData;
    /** @private */
    private _data;
    /**
     * @protected
     * 是否允许编辑
     */
    protected _enableEdit: boolean;
    /** @private */ private _engine;
    /** @private */ private _options;
    /** @private */ private _virtualLine;
    /** @private */ private _virtualLineDataSource;
    /** @private */ private _helpers;
    /** @private */ private _helpersDataSource;
    /** @private */ private _spline;
    /** @private */ private _splineDataSource;
    /** @private */ private _completedDataSource;
    /** @private */ private _completedElement;
    /** @private */ private _isAdd;
    /** @private */ private _selectedIndex;
    /** @private */ private _transformIndex;
    /** @private */ private _virtualVertex;
    _clampToGround: boolean;
    /** @private */
    private initSpline;
    /** @private */
    private initVirtualLine;
    /** @private */
    private initHelpers;
    /** @private */
    private initCompeleteElement;
    /** @private */
    private graphInfo;
    /** @private */
    private _handleCompleteClick;
    private set vertex(value);
    /** @private */
    private get vertex();
    /** @private */
    private _handleAddClick;
    private set virtualVertex(value);
    /** @private */
    private get virtualVertex();
    /** @private */
    private _handleAddMove;
    /** @private */
    private _handleAddFinish;
    /** @private */
    private _handleNodeClick;
    /**
     * @private
     * 拖拽时触发
     */
    private _handleTransformChanging;
    /**
     * @private
     * 拖拽结束后触发
     */
    private _handleTransformChanged;
    /** @private */
    private _getNodeNormal;
    /** @private */
    private addNode;
    /** @private */
    private updateNode;
    /** @private */
    private overwriteNodes;
    /** @private */
    private clearObjects;
    dispose(): void;
    /** 绘制的线数据 */
    set data(value: any);
    get data(): any;
    _vertex: any;
    /** @private */
    private get length();
}
import { Editor } from './Editor';
