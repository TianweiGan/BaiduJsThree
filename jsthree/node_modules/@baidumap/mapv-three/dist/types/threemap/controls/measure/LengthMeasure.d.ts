/**
 * 距离量测
 * ```jsx
 * const tiles = engine.add(new mapvthree.Default3DTiles({
 *  url: tilesetUrl,
 * }));
 * tiles.addEventListener('click', e => {});
 * tiles.addEventListener('mousemove', e => {});
 * const lengthMeasure = engine.add(new mapvthree.LengthMeasure(engine, {
 *  clampToGround: true,
 * }));
 * ```
 */
export class LengthMeasure extends Measure {
    /**
     * @param { Object } options 绘制参数
     * @param {string} options.lineColor 线颜色,默认为'rgb(200, 200, 50)'
     * @param {Object} options.virtualLineParameter 虚拟线参数，用来配置虚拟线的颜色、线宽等
     * @param {Object} options.lineParameter 线参数，用来配置线的颜色、线宽等
     * @param {number} options.fixedNum 保留几位小数，默认保留两位小数
     * @param {boolean} options.clampToGround 绘制时是否贴地
     * @param {boolean} options.showLabel 是否展示测量信息
     */
    constructor(engine: any, options?: {
        lineColor: string;
        virtualLineParameter: Object;
        lineParameter: Object;
        fixedNum: number;
        clampToGround: boolean;
        showLabel: boolean;
    });
    /** @private */ private _editor;
    /** @private */ private _node;
    /** @private */ private _infoMarker;
    /** @private */ private _infoMarkerDataSource;
    /** @private */ private _totalMarker;
    /** @private */ private _totalMarkerDataSource;
    /** @private 总长度 */
    private _length;
    /** @private 分段距离 */
    private _segmentLengths;
    /** @private */ private _indexOffset;
    /** @private */
    private _infoMarkerData;
    _totalMarkerData: any[];
    _engine: any;
    _options: {
        lineColor: string;
        virtualLineParameter: Object;
        lineParameter: Object;
        fixedNum: number;
        clampToGround: boolean;
        showLabel: boolean;
    };
    _showLabel: any;
    fixedNum: any;
    /** @private */
    private initEditor;
    /** @private */
    private initInfoMarker;
    /**
     * 生成分段距离的标注
     * @param {number} feature 分段距离
     * @returns 分段距离的标注
     */
    createInfoMarker: (feature: number) => HTMLDivElement;
    /**
     * 生成总长度的信息标注
     * @param {number} feature 总长度
     * @returns 总长度的信息标注
     */
    createTotalMarker: (feature: number) => HTMLDivElement;
    clearBuffer(): void;
    /**
     * 清除绘制元素
     */
    clearAll(): void;
    dispose(): void;
    /** @private */
    private get selectedIndex();
    /**
     * 总长度
     */
    get length(): number;
    get segmentLengths(): any[];
    set showLabel(value: any);
    get showLabel(): any;
}
import { Measure } from './Measure';
