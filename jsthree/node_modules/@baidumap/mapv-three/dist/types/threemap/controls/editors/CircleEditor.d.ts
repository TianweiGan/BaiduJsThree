/**
 * 圆形编辑器
 */
export class CircleEditor extends Editor {
    /**
     * @param { Object } options 绘制参数
     * @param {string} options.circleColor 圆形颜色,默认为'rgb(200, 200, 50)'
     * @param {Object} options.editingPolygonParameter 绘制中的圆的参数，用来配置虚拟圆的颜色等
     * @param {Object} options.editedPolygonParameter 绘制完成后的圆参数，用来配置圆的颜色等
     */
    constructor(engine: any, options?: {
        circleColor: string;
        editingPolygonParameter: Object;
        editedPolygonParameter: Object;
    });
    /** @private */
    private _circleData;
    /** @private */
    private _virtualLineData;
    /** @private */
    private _helpersData;
    /**
     * @protected
     * 是否允许编辑
     */
    protected _enableEdit: boolean;
    /** @private */ private _hasBeenMove;
    /** @private */ private _engine;
    /** @private */ private _options;
    /** @private */ private _circle;
    /** @private */ private _circleDataSource;
    /** @private */ private _virtualLine;
    /** @private */ private _virtualLineDataSource;
    /** @private */ private _helpers;
    /** @private */ private _helpersDataSource;
    /** @private */ private _completedDataSource;
    /** @private */ private _completedElement;
    /** @private */ private _isAdd;
    /** @private */ private _selectedIndex;
    /** @private */ private _transformIndex;
    /** @private */ private _raycaster;
    /** @private */ private _tempVector3;
    /** @private */ private _mapPlane;
    /** @private */
    private initCircle;
    /** @private */
    private initvirtualLine;
    /** @private */
    private initHelper;
    /** @private */
    private initCompletedElement;
    /** @private */
    private graphInfo;
    /** @private */
    private _handleCompleteClick;
    set vertex(value: any);
    get vertex(): any;
    /** @private */
    private _handleAddClick;
    /** @private */
    private _handleAddFinish;
    /** @private */
    private _handleNodeClick;
    /** @private */
    private _handleTransformChanging;
    /** @private */
    private _handleTransformChanged;
    /** @private */
    private _handleAddMove;
    /** @private */
    private calcLength;
    /** @private */
    private addNode;
    /**
     * @private
     * 移动圆心
     */
    private moveNode;
    /**
     * @private
     * 调整绘制圆的半径
     */
    private adjustRadius;
    dispose(): void;
    /** @private */
    private clearObjects;
    /** @private */
    private overwriteNodes;
    _getProperties(attributes: any): {
        scale: any;
        size: any;
    };
    _vertex: any;
    set data(value: any);
    /**
     * 绘制的圆形的数据信息
     */
    get data(): any;
}
import { Editor } from './Editor';
