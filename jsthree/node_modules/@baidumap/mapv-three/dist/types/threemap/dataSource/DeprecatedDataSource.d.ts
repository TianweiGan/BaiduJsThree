export class DeprecatedDataSource extends DataSource {
    constructor(options: any);
    isDirectBuffer: boolean;
    type: string;
    add(dataItem: any): void;
    remove(dataItem: any): void;
    /**
     * 设置数据
     * @param {Array} data 数据
     */
    setData(data?: any[]): void;
}
import { DataSource } from './DataSource';
