export class Strategy {
    constructor(options?: {});
    /** @private */
    private _logLoadInCurrentFrame;
    /** @private */
    private _loadSiblings;
    beginFrame(): void;
    endFrame(): void;
    computeTiles(engine: any, cameraPosition: any, frustum: any, renderState: any, focusPosition: any): {
        selectedTiles: never[];
        requestTilesQueue: never[];
    };
    _pushSelectedTile(selectedTiles: any, tile: any, isMeetSSE?: boolean): void;
    get surface(): any;
}
