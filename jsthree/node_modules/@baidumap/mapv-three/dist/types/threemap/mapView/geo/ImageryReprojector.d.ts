export class ImageryReprojector {
    /** @private */
    private _initResource;
    _renderTarget: any;
    _geometry: any;
    _material: ImageryReprojectMaterial | undefined;
    _mesh: any;
    /**
     * 重投影的瓦片包围盒不变，只是纹理坐标变化
     * @param {*} renderer
     * @param {*} tile
     * @param {*} texture
     * @param {*} outputTexture
     * @param {*} sourceProjection
     * @param {*} targetProjection
     * @returns
     */
    reprojectImagery(renderer: any, tile: any, texture: any, outputTexture: any, sourceProjection: any, targetProjection: any): any;
    dispose(): void;
}
export namespace ImageryReprojector {
    let DEFAULT: ImageryReprojector;
}
import { ImageryReprojectMaterial } from '../material/ImageryReprojectMaterial';
