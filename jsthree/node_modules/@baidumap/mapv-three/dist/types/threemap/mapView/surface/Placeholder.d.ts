export class Placeholder {
    constructor(options?: {});
    /** @private */
    private _maxCacheSize;
    _cache: LRUCache;
    _material: PlaceholderMaterial | undefined;
    createMesh(tile: any): any;
    _geometry: any;
    get(tile: any): any;
    clear(): void;
    handleRemove: (mesh: any, tileKey: any) => void;
    set maxCacheSize(value: number);
    get maxCacheSize(): number;
    set backgroundColor(value: any);
    get backgroundColor(): any;
    set borderColor(value: any);
    get borderColor(): any;
}
import { LRUCache } from '../../../util/LRUCache';
import { PlaceholderMaterial } from '../../../threejs/materials/map/PlaceholderMaterial';
