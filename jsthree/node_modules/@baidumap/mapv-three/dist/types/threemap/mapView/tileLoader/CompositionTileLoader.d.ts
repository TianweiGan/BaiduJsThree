export class CompositionTileLoader extends TileLoader {
    static IMAGERY: number;
    static BINARY: number;
    /** @private */
    private _loaders;
    /**
     * 确定瓦片由哪种loader加载
     * @param {*} tile
     */
    determineLoader(tile: any): number;
    createWorker(): void;
    _getRealTileLoader(tile: any): any;
    loadTile(tile: any): Promise<any>;
}
import { TileLoader } from './TileLoader';
