/**
 * 天地图影像瓦片提供者，用于加载和渲染天地图的影像瓦片数据。
 * 支持Web墨卡托和地理坐标两种投影方式。
 *
 * 主要功能：
 * - 支持天地图影像瓦片加载
 * - 支持Web墨卡托和地理坐标投影
 * - 支持瓦片URL的自动生成
 *
 * 需要使用天地图的TK，请在{@link TiandituConfig}中设置。
 * @category MapView
 * @example
 * ```javascript
 * // 创建天地图影像瓦片提供者
 * const provider = new TiandituImageryTileProvider({
 *     // 配置选项
 *     tk: 'your_tianditu_token',
 * });
 * ```
 */
export class TiandituImageryTileProvider extends ImageryTileProvider {
    /**
     * @param {object} options 配置选项
     * @param {string} [options.tk] 天地图密钥，如果不提供则使用 TiandituMapConfig.tk
     */
    constructor(options: {
        tk?: string | undefined;
    });
    /**
     * 瓦片提供者名称
     * @readonly
     */
    readonly name: "TiandituImageryTileProvider";
    /**
     * 天地图密钥
     * @private
     */
    private _tk;
    /**
     * 支持的投影方式
     * @private
     */
    private _supportedTargetProjectionNames;
    /**
     * 默认最大缩放级别
     * @private
     */
    private _defaultMaxLevel;
    /**
     * 获取瓦片URL
     * @internal
     * @param {number} z 缩放级别
     * @param {number} x 瓦片X坐标
     * @param {number} y 瓦片Y坐标
     * @param {object} tile 瓦片对象
     * @returns {string|boolean} 瓦片URL，如果缩放级别为0则返回false
     */
    getTileURL(z: number, x: number, y: number, tile: object): string | boolean;
}
import { ImageryTileProvider } from './ImageryTileProvider';
