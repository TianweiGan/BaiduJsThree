/**
 * 地形瓦片提供者，负责地形瓦片数据的请求和缓存。<br/>
 * 该类是所有地形瓦片提供者的**抽象基类，不可直接使用，需要使用子类。**
 *
 * @category Internal
 */
export class TerrainTileProvider extends TileProvider {
    /**
     * @param {*} tile
     * @returns
     * geometry
     * @desc 请求瓦片数据
     */
    doRequestTileData(tile: any): Promise<void>;
}
export namespace TerrainTileProvider {
    let heightmapTerrainQuality: number;
    /**
     * @private
     * 当geometry来自高度图时，确定适当的几何误差估计。
     *
     * @param {Ellipsoid} ellipsoid The ellipsoid to which the terrain is attached.
     * @param {number} tileImageWidth The width, in pixels, of the heightmap associated with a single tile.
     * @param {number} numberOfTilesAtLevelZero The number of tiles in the horizontal direction at tile level zero.
     * @returns {number} An estimated geometric error.
     */
    function getEstimatedLevelZeroGeometricErrorForAHeightmap(ellipsoid: Ellipsoid, tileImageWidth: number, numberOfTilesAtLevelZero: number): number;
}
import { TileProvider } from '../TileProvider';
