/**
 * @file SurfaceTileManager
 * 负责根据输入投影和瓦片，从provider中找出需要的数据
 * 不负责瓦片的具体请求和缓存清理
 */
export class SurfaceTileManager {
    constructor(surface: any);
    /** @private */
    private _surface;
    _pendingCount: number;
    getSurfaceTile(tile: any): void;
    requestSurfaceTile(tile: any): void;
    dispose(): void;
    get pendingCount(): number;
}
