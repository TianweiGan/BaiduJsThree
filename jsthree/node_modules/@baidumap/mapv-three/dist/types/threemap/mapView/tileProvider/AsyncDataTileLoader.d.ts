export class AsyncDataTileLoader {
    constructor(provider: any, workerClass: any, size: any);
    provider: any;
    _workerTaskScheduler: WorkerTaskScheduler;
    isMessageCompleted(data: any, message: any): boolean;
    getResponseMessageId(data: any, message: any): any;
    _vectorToArray(vector: any): any[];
    _boxToArray(box: any): any[];
    /**
     * 请求瓦片
     *
     * @param {Tile} tile 待请求的瓦片对象
     * @returns {Promise<void>} 返回一个Promise，处理完成后返回void类型的数据
     */
    requestTile(tile: Tile): Promise<void>;
    /**
     * 向所有 worker 发送消息。
     *
     * @param message 消息内容
     * @param transferableData 可移动数据
     */
    postMessageToAll(message: any, transferableData: any): void;
}
import { WorkerTaskScheduler } from '../../../util/WorkerTaskScheduler';
