export class HierarchicalTileLoaderStrategy extends Strategy {
    isHierarchicalTileLoaderStrategy: boolean;
    computeTiles(engine: any, cameraPosition: any, frustum: any, renderState: any): {
        selectedTiles: any[];
        requestTilesQueue: any[][];
        placeholderTiles: never[];
    };
    queueTileLoaded(tile: any, requestTiles: any): void;
    /**
     * @param {*} cameraPosition
     * @param {*} frustum
     * @param {*} tile
     * @param {*} selectedTiles
     * @param {*} requestTiles
     * @param {*} requestSiblingTiles
     * @param {*} renderState
     * @returns true 瓦片可渲染， false 瓦片不可渲染 undefined 瓦片不在视野里
     */
    _computeHierarchicalIntersectTiles: (cameraPosition: any, frustum: any, tile: any, selectedTiles: any, requestTiles: any, requestSiblingTiles: any, lowPriorityTiles: any, highPriorityTerrainTiles: any, renderState: any) => boolean;
}
import { Strategy } from './Strategy';
